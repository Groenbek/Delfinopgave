package people;

import mappers.MemberMap;
import presentation.ConsoleUI;
import presentation.ConsoleUI;

public class Formand {

    private MemberMap mc = new MemberMap();
    private ConsoleUI ui = new ConsoleUI();
   
    /*
    private Member createMember(){
    ui.println("ID: ");
    int id = Integer.valueOf(ui.getInput());
    ui.println("Alder: ");
    int age = Integer.valueOf(ui.getInput());
    ui.println("Navn: ");
    String name = ui.getInput();
    ui.println("Aktivitet: ");
    String act = ui.getInput();
    Member returnMember = new Member(id, age, name, act);
    return returnMember;
    }
    */
    
    /* public void insertMember(){
    mc.insertMember(createMember());
    }*/
    
    
    
    
}
